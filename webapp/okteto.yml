name: webapp
selector:
  app: webapp
image: okteto/golang:1
command: bash
securityContext:
  capabilities:
    add:
    - SYS_PTRACE
sync:
- .:/usr/src/app
forward:
- 2345:2345
- 8081:8081
volumes:
- /go/pkg/
- /root/.cache/go-build/
